image: docker

services:
    - docker:dind
    
stages:
    - test
    - deploy
    
test:
    stage: test
    only:
        - master
        - dev
    script:
        - apk add python python-dev py-pip build-base
        - pip3 install -q -r chefsApprentice/requirements.txt
        - python3 manage.py test
        
    
step-deploy-prod:
    stage: deploy
    only:
        - master
    script:
        - apk add python python-dev py-pip build-base
        - pip3 install -q -r chefsApprentice/requirements.txt
        - pip3 install docker-compos
        - docker-compose up -d --build
    environment: master
    when: manual
            