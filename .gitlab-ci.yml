image: docker

services:
    - docker:dind
    
stages:
    - test
    - deploy
    
test:
    stage: test
    only:
        - master
        - dev
    script:
        - apk add python python-dev python pip
        - pip install -q -r chefsApprentice/requirements.txt
        - python manage.py test
        
    
step-deploy-prod:
    stage: deploy
    only:
        - master
    script:
        - apk add python python-dev python pip
        - pip install -q -r chefsApprentice/requirements.txt
        - pip install docker-compos
        - docker-compose up -d --build
    environment: master
    when: manual
            